syntax = "proto3"; // proto3

message MyMessage { // define messages
  int32 id = 1;
  string first_name = 2; // string is field type, first_name is field naem, 2 is Field Tab (e.g. Number)
  bool is_valid = 3;
}